# Every target/, not only the workspace root's: a nested
# crates/*/target/ escaped `/target` and a test's data dir landed in the
# index, setup code and all.
target/
node_modules/
dist/
*.sqlite
*.sqlite-*
.env
.DS_Store
/overmind-data
